# Enba Similasyon — AI El Teslimi
> Tarih: 2026-05-09
> Hazırlayan: Claude Sonnet 4.6 (bu oturumun sonu)
> Devam eden AI: bir sonraki model

---

## Proje Özeti

**Enba Similasyon** — Geri dönüşüm ve üretim sektörü için Türkçe çok kiracılı ERP platformu.

- **Stack:** React 18 + TypeScript + Vite + Tailwind CSS + Supabase
- **Repo:** `c:\Users\Fujitsu\OneDrive\Desktop\Enba Similasyon`
- **Canlı URL:** https://uygulama.basarunal.com (Vercel)
- **Demo:** `demo@enba.com`
- **Kullanıcı email:** y.basarunal@gmail.com

---

## Bu Oturumda Yapılanlar (2026-05-09)

### FastPlan iyileştirmeleri — commit `0187c30`

Dosya: `src/modules/FastPlan.tsx`

**Eklenen özellikler:**

1. **Başabaş noktası** — `hesapla()` fonksiyonuna `basabasNokta: number` eklendi (ton/ay, Infinity = kâra geçilemiyor). Form sağ panelde "Yatırım Analizi" kartında gösteriliyor.

2. **Geri ödeme süresi** — `geriOdemeSuresi: number | null` eklendi (ay cinsinden, null = CAPEX yok veya zarar). Renk kodlu: ≤36 ay yeşil, ≤60 sarı, üstü kırmızı.

3. **Kapasite kullanımı (başabaş)** — Mevcut ton / başabaş ton oranı %, Yatırım Analizi kartında gösteriliyor.

4. **Gider dağılım bar chart** — CSS-based (harici kütüphane yok). 5 kategori: Mal Alım / Nakliye / Personel / Enerji / Diğer. Sağ panelde P&L'den sonra gösteriliyor.

5. **Duyarlılık tablosu** — Satış fiyatı ±%20 senaryoları (−20, −10, 0, +10, +20). `useMemo` ile memoize. Sağ panelin en altında.

6. **Karşılaştırma % sütunu** — Karşılaştırma tablosu 5. sütuna `% (göreli fark)` eklendi. `farkRel()` yardımcı fonksiyonu eklendi.

7. **Versiyon notu** — `PlanVersion` interface'ine `not?: string` eklendi. SaveModal'da input alanı mevcut. Versiyon dropdown'da "V1 · 01.05.2026 · not metni" formatında gösteriliyor.

8. **Plan kartı birim maliyet** — EBITDA badge yanına `₺X.XXX/ton` gösterimi eklendi.

---

## Mimari — Kritik Kararlar

### Navigasyon (React Router YOK)
Custom history stack — `App.tsx`:
- `activeModule` → hangi modül render edilir
- `navigate(moduleId)` → her modüle prop olarak geçilir
- **URL hiç değişmez** — embed/iframe uyumluluğu için

### State
1. **Supabase** → auth, profiles, tasks, assets, permits, plans, HR, arşiv, stok, satış, üretim, lojistik, nakit akışı
2. **localStorage** → FastPlan/DetailedPlan önbellek (`enba_fast_plans_v2`, `enba_detailed_plans`), OAuth tokenlar, tema, dil
3. **useState** → modül bazlı geçici durum

### FastPlan Veri Akışı
```
usePlanSync (hook) → localStorage önbellek + Supabase sync
PlanCard → { id, baslik, params: PlanParams, sonuc: PlanSonuc, versions: PlanVersion[] }
hesapla(PlanParams): PlanSonuc → pure function, side-effect yok
```

### Auth
Supabase email/şifre. `profiles` satırı:
- `company_id` → tenant
- `role` → `'super_admin' | 'admin' | 'user'`
- `permissions` → modül bazlı boolean RBAC

---

## Öncelik Sırası (Kullanıcı Kararı — 2026-05-08)

1. **FastPlan iyileştirmeleri** ← BU OTURUMDA TAMAMLANDI
2. **DetailedPlan iyileştirmeleri** ← SIRADAKI
3. **Paraşüt entegrasyonunu tamamla** — stok eşleştirme, yazma uç noktaları
4. **PnL analizi güçlendirme**
5. **Yapay zeka asistanı** — siteye entegre AI chat

**Ertelenenler (müşteri talebi olmadıkça dokunma):**
- Bordro/HR hesaplama
- Muhasebe motoru
- E-fatura

---

## Bir Sonraki Görev: DetailedPlan İyileştirmeleri

`src/modules/DetailedPlan.tsx` — önce dosyayı oku, analiz et.

FastPlan'da yapılanları referans al:
- Hesaplama eksikleri var mı? (başabaş, geri ödeme, duyarlılık)
- Versiyonlama nasıl? (not alanı, delta özeti)
- Karşılaştırma var mı?
- Görsel: gider dağılım, PDF

---

## Kritik Dosyalar

| Dosya | İçerik |
|-------|--------|
| `src/modules/FastPlan.tsx` | Hızlı iş planı — 1600+ satır |
| `src/modules/DetailedPlan.tsx` | Detaylı iş planı |
| `src/hooks/usePlanSync.ts` | Plan localStorage ↔ Supabase sync |
| `src/api/supabase.ts` | Supabase client + profileAPI (10s cache) |
| `src/api/dataService.ts` | Genel CRUD + StockRow/SalesRow typed |
| `src/App.tsx` | Custom router + lazy-load + ErrorBoundary |
| `src/components/ErrorBoundary.tsx` | Lazy-load crash fallback |

---

## Tamamlanan Altyapı (2026-05-08)

Bunlara tekrar dokunma — tümü çalışıyor:

- ✅ Supabase RLS (migration_v24 + v24b) — business_plans
- ✅ usePlanSync toplu upsert/insert
- ✅ React.lazy 22 modül + Suspense + ErrorBoundary
- ✅ PnL html2pdf dynamic import
- ✅ profileAPI cache invalidation (updateProfile + adminUpdateProfile)
- ✅ Calendar → Supabase tasks (localStorage bypass fix)
- ✅ Archive + HR → useSupabaseQuery hook
- ✅ console.log temizliği
- ✅ TypeScript any azaltma (usePlanSync, dataService)

---

## Veri Güvenliği — KRİTİK KURAL

Uygulama gerçek veri girişi aşamasında. **Hiçbir geliştirme mevcut veriyi kaybettiremez.**

- localStorage key'leri değiştirme: `enba_fast_plans_v2`, `enba_detailed_plans`, `enba_language`, `enba_theme`
- Supabase'de destructive işlem öncesi tablo export al
- Migration öncesi SELECT ile test et, onay al

---

## Çalıştırma Komutları

```bash
npm run dev        # Vite dev server (port 5173)
npm run build      # tsc + vite build
# npm run lint — ESLint global yüklü değil, node_modules'de de yok
npx tsc --noEmit   # Tip kontrol (asıl doğruluk kontrolü)
```

---

## Obsidian Wiki

Wiki: `Enba_Obsidian_Vault/`
- `log.md` — append-only geliştirme günlüğü
- `index.md` — wiki sayfaları indeksi
- `Memory/MEMORY.md` — kalıcı hafıza indeksi

Her önemli değişiklikten sonra `log.md`'ye giriş ekle.

---

## Son Git Durumu

```
branch: main
son commit: 0187c30 — feat: FastPlan hesaplama + görsel iyileştirmeleri
pushed: evet (github)
```

---

## Bekleyen Teknik Görev

- [ ] **Admin test hesabı** — Supabase'de `role='admin'` + geçerli `company_id` → şirket izolasyonu test
- [ ] **Paraşüt tamamlama** — Enba stok ↔ Paraşüt item eşleştirme, yazma uç noktaları
