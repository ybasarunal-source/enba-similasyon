# Enba Similasyon — AI El Teslimi 11 Mayıs 2026

Bu belge, 10–11 Mayıs 2026 oturumlarında yapılan her değişikliği ve devam eden konuları kapsar.

---

## 1. Bu Oturumlarda Tamamlanan İşler

### A. DetailedPlan Hesaplama Hataları Düzeltildi (`c45cf04`)
**Dosyalar:** `src/utils/detailedPlanCalculations.ts`, `src/modules/planning/PlanningWizard.tsx`, `src/modules/planning/steps/PersonnelStep.tsx`

- Asgari ücret fallback'leri yanlıştı: `17002` → `28075.5` (net), `5000` → `12799.13` (SGK)
- Amortisman hardcode'du (`/ 60`); artık her yatırımın kendi `geriOdeme` alanı kullanılıyor
- `PersonnelStep` + `PlanningWizard` DEFAULT_PLAN_DATA da güncellendi
- Kullanılmayan importlar temizlendi (`useTranslation`, `ChevronDown`, `ExternalLink`)

### B. ReportStep — Yatırım Analizi + Birim Maliyet Analizi (`50fca7f`)
**Dosya:** `src/modules/planning/steps/ReportStep.tsx`

Gider Dağılımı ile 5-Yıllık Projeksiyon arasına iki yeni kart eklendi:
- **Yatırım Analizi kartı**: başabaş noktası (ton/ay), geri ödeme süresi (ay), kapasite kullanımı (%), CAPEX toplamı — `calculateDetailedPlan`'ın `investmentAnalysis` çıktısından besleniyor
- **Birim Maliyet Analizi kartı**: ortalama satış ₺/ton, ortalama maliyet ₺/ton, katkı payı ₺/ton + 12 aylık tablo
- `fmtDec` formatter eklendi (ondalıklı gösterim)
- Dangling `useTranslation` çağrısı (TS 2304 hatası) silindi

### C. Sabit Değerler Merkezi Dosyaya Taşındı (`9ae216f`)
**Yeni dosya:** `src/utils/constants.ts`

```ts
export const ASGARI_NET = 28075.5;
export const ASGARI_SGK = 12799.13;
export const DEFAULT_DAILY_MEAL = 200;
export const DEFAULT_WORK_DAYS = 26;
export const DEFAULT_ELECTRIC_PRICE = 2.5;
```

Bu sabitleri kullanan 4 dosya güncellendi: `FastPlan.tsx`, `detailedPlanCalculations.ts`, `PlanningWizard.tsx`, `PersonnelStep.tsx`. Asgari ücret değiştiğinde tek yerden güncellenir.

### D. PnL — Al Sat Toplam (M149) Kaldırıldı (`83fb8ac`)
**Dosya:** `src/modules/PnL.tsx`

- `M149 "Al Sat Toplam"` satırı PNL_CONFIG'den silindi — Türk muhasebe planında 150 hesabı stok (bilanço) hesabıdır, gelir hesabı değil; kavramsal hata
- `Toplam Satış (M179)` formülü `["M109", "M149", "M159"]` → `["M109", "M159"]` olarak düzeltildi
- Her iki mapping bloğundan `rawKat.includes('150') → M149` satırı silindi

### E. PnL — Sağ Sütun Toplamları + Buton Etiketleri (`b33f059`, `fb61427`)
**Dosya:** `src/modules/PnL.tsx`

- En sağdaki "Yıllık Toplam" sütunu doğru hesaplanacak şekilde dolduruldu
- Upload butonlarına daha açıklayıcı etiketler eklendi

---

## 2. Kritik Mimari Notlar (Değişmeyen)

- **React Router YOK** — tüm navigasyon `App.tsx`'te custom history stack
- **Zustand/Redux YOK** — state: Supabase + localStorage + useState
- **localStorage key'leri değiştirilmez**: `enba_fast_plans_v2`, `enba_detailed_plans`, `enba_language`, `enba_theme`
- **ESLint sıfır tolerans** — her committen önce `npm run lint`
- **TypeScript strict** — `npx tsc --noEmit` ile kontrol et

---

## 3. Mevcut Kod Yapısı — İlgili Dosyalar

| Dosya | Durum |
|-------|-------|
| `src/utils/constants.ts` | **YENİ** — tüm sabit değerler buraya |
| `src/utils/detailedPlanCalculations.ts` | Düzeltildi — doğru maaş + amortisman |
| `src/modules/planning/steps/ReportStep.tsx` | Yatırım + birim maliyet kartları eklendi |
| `src/modules/FastPlan.tsx` | constants.ts import ediyor |
| `src/modules/PnL.tsx` | M149 kaldırıldı, sağ sütun toplamları düzeltildi |

---

## 4. Devam Eden / Açık Konular

### PnL — Toplam Satış Rakamı
Kullanıcı toplam satışın **7.084.243,67 ₺** olması gerektiğini belirtti. Araştırılan 3 olası neden:

1. `net_total` (KDV hariç) kullanılıyor — eğer beklenti KDV dahil (`gross_total`) ise fark ~%20
2. Dövizli faturalarda `total_trl` hesabı net mi gross mu belirsiz ([parasut.ts:279](../src/api/parasut.ts))
3. Bazı faturalar Paraşüt category_name eşleşmesine takılmıyor, M179 dışında kalıyor

**Teşhis için gereken:** Kullanıcıdan M179'un şu an gösterdiği rakam + 7,084,243.67 rakamının hangi Paraşüt ekranından geldiği (net mi, gross mu?)

### DetailedPlan — Kayıt Mekanizması
Bir sonraki adım: DetailedPlan versiyonlama + Supabase sync derinleştirme.

---

## 5. Öncelik Sırası (Kullanıcı Kararı)

1. **PnL toplam satış doğruluğu** — açık sorun
2. **DetailedPlan kayıt mekanizması** — versiyonlama + Supabase sync
3. **Paraşüt tamamlama** — stok eşleştirmesi, kalan uç noktalar
4. **Yapay zeka asistanı** — siteye entegre AI chat
